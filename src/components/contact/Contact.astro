---
import { Mail, MapPin, Phone } from "@lucide/astro";
import SectionHeader from "../ui/SectionHeader.astro";
import ContactForm from "./ContactForm.astro";
import ContactInfo from "./ContactInfo.astro";
import MapEmbed from "./MapEmbed.astro";

const SHOW_CONTACT_FORM = false;

// Static mock content - will be replaced with CMS data later
const content = {
  title: "Kontakt",
  subtitle: "Skontaktuj sie z nami",
  address: "ul. Przykladowa 123, 00-000 Warszawa",
  phone: "+48 123 456 789",
  email: "kontakt@studiotapicerskie.pl",
  formLabels: {
    name: "Imie i nazwisko",
    email: "Adres e-mail",
    message: "Wiadomosc",
    consent: "Wyrazam zgode na przetwarzanie moich danych osobowych zgodnie z",
    privacyPolicy: "Polityka Prywatnosci",
    send: "Wyslij wiadomosc",
  },
  mapSource:
    "https://www.google.com/maps/embed?pb=!1m18!1m12!1m3!1d2443.5283457!2d21.012229!3d52.229676!2m3!1f0!2f0!3f0!3m2!1i1024!2i768!4f13.1!3m3!1m2!1s0x0%3A0x0!2zNTLCsDEzJzQ2LjgiTiAyMcKwMDAnNDQuMCJF!5e0!3m2!1sen!2spl!4v1234567890",
};

const contactItems = [
  { icon: MapPin, label: content.address },
  { icon: Phone, label: content.phone },
  { icon: Mail, label: content.email },
];
---

<section id="contact" data-testid="contact" class="bg-secondary py-20">
  <div class="mx-auto max-w-7xl px-4 sm:px-6 lg:px-8">
    <SectionHeader title={content.title} subtitle={content.subtitle} />

    {SHOW_CONTACT_FORM ? (
        <div class="grid gap-12 md:grid-cols-2">
          <div>
            <ContactInfo items={contactItems} class="mb-12" />
            <MapEmbed src={content.mapSource} title="Lokalizacja" />
          </div>

          <div>
            <ContactForm labels={content.formLabels} />
          </div>
        </div>
      ) : (
        <div class="grid gap-12 md:grid-cols-2">
          <div>
            <ContactInfo items={contactItems} />
          </div>

          <div data-animate="contact-map">
            <MapEmbed src={content.mapSource} title="Lokalizacja" />
          </div>
        </div>
      )}
  </div>
</section>

<style>
  @media (scripting: enabled) {
    [data-animate="contact-map"] {
      opacity: 0;
      transform: scale(0.95);
    }
  }
</style>

<script>
  import { animate, inView } from "motion";

  inView("[data-animate='contact-map']", (element) => {
    animate(
      element,
      { opacity: 1, scale: 1 },
      { duration: 0.6, ease: "easeOut", delay: 0.3 }
    );
  });
</script>
